import { describe, it } from 'vitest'

describe('DynamicFields', () => {
  it('renders message in default locale', () => {})
  it('click changes to selected locale', () => {})
  it('reverts back to original locale', () => {})
  it('locale change is rendered collectly', () => {})
})
