class FieldDependancy {
  stage: number
  index: number
  value: number

  constructor(stage: number, index: number, value: number) {
    this.stage = stage
    this.index = index
    this.value = value
  }
}

export default FieldDependancy
